server:
  port: 8082
  error:
    whitelabel:
      enabled: off
spring:
  cache:
    type: redis
  application:
    name: admin-service
  profiles:
    active: docker
  data:
    redis:
      repositories:
        enabled: false